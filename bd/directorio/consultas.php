<?php
// Consulta que muestra todas las personas del directorio

$sql = "SELECT * FROM directorio";

   

    // $query = $pdo -> prepare($sql);
    // $query -> execute();

    // $results = $query -> fetchAll(PDO::FETCH_OBJ);